# AGENTS.md

> 이 파일은 `intionma/char-gallery-pages`에서 작업하는 사람과 AI 에이전트가 가장 먼저 읽어야 하는 단일 기준(source of truth)이다.

## 운영 기준

- 이 저장소는 캐릭터 갤러리의 현재 본진이며, 오너가 종료를 명시할 때까지 모든 신규 기능·데이터·UI 작업은 여기에서 진행한다.
- 기존 `intionma/other/char-gallery`는 동결된 레거시·참고용이다. 오너의 명시적 지시 없이 양쪽 저장소를 동시에 수정하거나 자동 동기화하지 않는다.
- 현재 운영 배포 대상은 GitHub Pages(<https://intionma.github.io/char-gallery-pages/>)다.
  Vercel을 쓰지 않으므로 `intionma/other`의 Vercel Hobby 100건/일 공용 한도와 무관하고,
  `other`에서 분리된 단독 저장소라 머지가 다른 프로젝트 빌드를 유발하지 않는다.
- **머지에 건별 승인이 필요 없다.** 작업을 끝내고 검증을 통과했으면 오너에게 다시 묻지 말고
  PR을 머지해 배포까지 마친다. 작업 지시 자체가 승인이다. 이 권한은 이 저장소에만 적용되며,
  `intionma/other`에는 여전히 오너의 명시적 승인이 필요하다.
- 다만 되돌리기 어려운 작업(강제 push로 남의 커밋 폐기, 파일·데이터 대량 삭제, 저장소 설정
  변경)은 여전히 먼저 확인한다. 배포 자체는 이 범주가 아니다.
- 검증은 배포가 아니라 로컬 빌드와 정적 검사로 수행한다. 배포 한도가 없어졌다고 해서
  "일단 배포해서 확인"하거나 재배포용 트리거·빈 커밋을 만들지는 않는다.

## 공개 저장소 원칙

- 이 저장소는 공개 GitHub Pages 정적 사이트다.
- 비밀값, 개인 정보, 내부 운영 문서, 사적인 대화 내용을 커밋하지 않는다.
- 서버 함수와 데이터베이스를 추가하지 않는다.
- 이미지 파일을 저장하지 않고 공개 출처 URL만 사용한다.
- 배포 전 `node scripts/audit-public.mjs`를 실행한다.
- 변경은 로컬 검증 후 최소 커밋으로 반영한다.
- 데이터 출처와 원본 링크를 유지한다.
